server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/fakhrichaerul/config-server.git
          clone-on-start: true
          searchPaths: '{application}'

      




